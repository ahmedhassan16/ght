name: Copu files
outputs:
  value: ${{ '$' }}{{ toJSON(steps.run.outputs) }}
runs:
  using: composite
  steps:
  - name: customAction
    shell: bash
    run: echo "action 1 - ${{ github.event.repository.updated_at}}"