name: Test Actions
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: action1
        uses: /.github/actions/copyFiles
      - name: action2
        uses: /.github/actions/echoMessage